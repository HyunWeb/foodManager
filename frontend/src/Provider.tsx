const theme = extendTheme({
  // 여기서 기본 테마를 수정하거나, 사용자 정의 테마를 설정할 수 있습니다.
  config: {
    initialColorMode: "light", // 기본 색상 모드 (다크 모드 또는 라이트 모드)
    useSystemColorMode: true, // 시스템의 색상 모드를 따를지 여부
  },
});
